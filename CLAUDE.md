## Language Policy / 语言输出规则

- Default response language: Simplified Chinese (zh-CN).
- 除非用户明确要求使用其他语言，否则所有自然语言回复必须使用简体中文。
- 解释、计划、总结、问题、进度说明、代码审查意见、错误分析、提交信息、PR 描述默认都用简体中文。
- 不要中英混杂地写完整句子；只有技术名词、API 名称、库名、命令、文件路径、变量名、函数名、类名、错误信息、日志、协议名可以保留英文原文。
- 如果某个英文技术词有常用中文翻译，第一次出现时使用“中文（English）”格式，例如“上下文窗口（context window）”。
- 如果用户用中文提问，必须用中文回答。
- 如果用户用英文提问，但没有明确要求英文回答，仍然优先用简体中文回答。
- 如果用户明确要求英文、日文或其他语言，只在该次回复中使用用户指定语言；后续默认恢复简体中文。
- 代码本身不要为了语言规则而改名；标识符、已有注释、已有文档风格应遵循项目现有约定。
- 新增注释、文档、README 内容如果项目没有明确英文规范，默认使用简体中文。
- 回复前自检：除代码、命令、路径、错误日志、专有名词和引用原文外，所有自然语言段落都必须是简体中文。