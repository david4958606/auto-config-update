# 配置"自适应增量升级" — 设计方案

## Context（为什么做）

设备软件 = 软件主体 + 分层 XML 配置（Opr 工艺腔室 / Func 功能 / Phy 物理设备）。
启动时读配置、逐层实例化类并调用方法配置对象。每次发布新功能，所有待升级机台要
**手动改配置**启用功能；各机台"相似但不相同"。现状靠"改一个腔室 + 批量替换腔室编号
(Ch2→Ch3/4/5) + 大量人工检查"，脆弱易错；且一次发布含多个功能，并非每台都要全部。

**根因**：用"改文本 + 文本替换"做一件本质上"结构化、有语义"的事——`Ch2` 会误伤
`/IO/Ch2/...`、会漏改、需人工兜底。

**目标**：把升级从"文本编辑"变为"对解析后 XML 树的幂等语义补丁"，用自动校验取代
人工复查，并原生支持"多功能发布、按需启用"。

## 已定方向

- **主路线**：原地语义补丁——存量配置是唯一真相源，不做模板化重生成（迁移成本最低）。
- **功能选择**：自动适用性判断给默认值 + 每台机台清单 enable/exclude 覆盖。
- **校验**：把"有但不正式"的约定固化成可执行规则。
- **实现语言（推荐）**：Python + `lxml`（原生 XPath、保留注释/格式、干净 diff）。
  工具是离线工程工具，非设备运行时；除非要嵌入 C++ 运行时或单二进制分发，否则 Python 最快落地。

## 七个核心点子

### 1. 给"树"打补丁，不改文本
解析成 XML 树，用**结构路径**(class/type 层级)定位节点，而非按行/字符串匹配。
补丁 = "在匹配节点下确保存在某子节点 / 设某方法值"。消灭替换误伤与漏改。

### 2. 腔室编号是"上下文变量"，不是文本
补丁内容写 `/IO/{ch}/TurboPump/OnoffDO`，`{ch}` 由当前处理的腔室自动解析。
对 Ch3/4/5 套同一补丁各自填对编号——**批量替换从此不存在**。
前置功课：先列清楚各机台的**变化轴**（腔室编号 / IO 地址 / 设备型号 / 子系统有无），
把这几个轴参数化，其余视为不变量。

### 3. 幂等：重跑 = 没跑
每个动作先看现状，已达目标即 no-op。→ 重跑/失败重试安全；消灭"加没加/加重了"复查。

### 4. 自适应 = 适用性谓词
每个功能自带前置条件（XPath 测试），工具读现有配置自动判断该不该装。
例：分子泵增强的条件 = "该腔室下已挂 `PhyTurboPump`"。不靠人记。

### 5. 多功能按需启用（三层解耦）
- **发布目录**：一次发布 = 一组独立功能声明 + 版本号。
- **适用性谓词**：算出每台每腔室的默认功能集。
- **机台清单**：每台一份小文件，`enable` 补 / `exclude` 减，覆盖默认。
最终 = 适用性命中 ∪ enable − exclude。
再加**已应用账本**（配置里写元数据记 `功能@版本`），只补增量 → 真正的增量升级。

### 6. 自动校验取代人工检查
- **交叉引用完整性**：每个 `type="method"` 引用的 `./PhyXxx` 必须解析得到；
  IO 路径唯一不冲突；腔室子树内编号自洽（Ch3 子树残留 Ch2 立即报错）。
- **兄弟一致性检查**：同类型腔室升级后，各子树除已知变化轴外应结构全等；
  任何意料外的不对称几乎都是人为错误，直接标红。**无需"标准答案配置"，靠互比即可。**

### 7. 评审看"语义 diff"，不看文本 diff
落盘前 dry-run 输出人类可读摘要：
```
Ch3: 新增 Vacuum/PhyTurboPump.setOnoffSp → /IO/Ch3/TurboPump/OnoffDO
Ch5: 跳过（未检测到分子泵）
```
人只审意图。

## 补丁原语（幂等）
- `ensure_node(parent, tag, class, type)` / `set_method(node, name, value)`
- `ensure_child` / `update_method` / `remove_node`
- 值支持 `{ch}` 等上下文变量，由腔室作用域解析。

## 文件结构（建议）
- `src/model.py` 解析/寻址/腔室作用域与 `{ch}` 解析
- `src/ops.py` 幂等补丁原语
- `src/feature.py` 功能 YAML 加载 + 适用性谓词 + 依赖排序
- `src/manifest.py` 机台清单 + 发布目录合并
- `src/validate.py` 交叉引用 + 兄弟一致性 + 结构校验
- `src/diff.py` 语义 diff 报告
- `src/cli.py` `validate / plan / apply / diff`
- `features/<id>.yaml`、`machines/<machine>.yaml`、`tests/`

## 落地增量（每步独立可验证）
1. **MVP**：解析 + 3 个幂等原语 + `{ch}` 解析 + 语义 dry-run diff，拿一个真实功能跨多台验证。
2. **校验引擎**：交叉引用 + 兄弟一致性 + 固化结构约定。
3. **功能目录 + 机台清单 + 适用性谓词**：多功能、按需启用。
4. **已应用账本 + 依赖拓扑排序**：真正增量升级。

## 验证（end-to-end，用脱敏配置即可，不需真实机密）
1. 造一份脱敏 PVD 腔室配置（含 Ch1，无新功能）作夹具。
2. 写一个功能 apply 到多腔室，确认：节点正确插入、`{ch}` 按腔室填对、**再跑一次 no-op**。
3. 故意制造坏引用（method 指向不存在节点、重复 IO），确认 `validate` 报错。
4. 一台 `exclude` 该功能、一台无分子泵，确认前者跳过、后者适用性不命中。
5. `plan` 语义 diff 与 `apply` 结果一致；兄弟一致性检查能抓出人为不对称。

## 建议
先不上"模板重生成"大改造。以"原地幂等补丁 + 校验 + 语义 diff"起步，迁移成本最低、
第一天就能用真实功能跨多台验证收益；跑顺后再评估是否把腔室抽成模板。
